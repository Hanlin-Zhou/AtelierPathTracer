#pragma once

namespace Math {
	// constants
	/*float PI;
	float PI2;
	float DoverR;
	float RoverD;*/

	float ToRadian(float degree);
	float ToDegree(float radian);

	float Lerp(float t, float v1, float v2);
}